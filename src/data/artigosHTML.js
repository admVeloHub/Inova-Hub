/**
 * VeloHub V3 - Artigos HTML
 * Artigos extraídos dos arquivos HTML fornecidos
 * VERSION: v2.0.0 | DATE: 2025-12-09
 * 
 * IMPORTANTE: As imagens devem estar em media.images (array de strings com caminhos)
 * O HTML não deve conter tags <img> - as imagens são renderizadas separadamente via getAllImages()
 * Seguindo o mesmo padrão do Velonews
 */

export const artigosHTML = [
  // Artigos serão adicionados um por vez
];
